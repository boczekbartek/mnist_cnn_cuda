version: '3.7'
services:
  mnist-cpu:
    build:
      context: .
      dockerfile: Dockerfile-CPU
    image: mnist_cnn_cuda:${TAG}
  mnist-cpu-train:
    image: mnist_cnn_cuda:${TAG}
    environment:
      - PYTHONPATH=/
    volumes:
      - ./mnist_py:/mnist_py
    command: >
      python /mnist_py/train.py